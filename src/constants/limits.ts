export const MEDIA_LIMITS = {
  IMAGE_MAX_BYTES: 10 * 1024 * 1024,
  IMAGE_MAX_WIDTH: 1920,
  IMAGE_MAX_HEIGHT: 1080,
  IMAGE_JPEG_QUALITY: 0.85,

  VIDEO_CHAT_MAX_BYTES: 100 * 1024 * 1024,
  VIDEO_OTHER_MAX_BYTES: 50 * 1024 * 1024,
  VIDEO_MAX_RESOLUTION_HEIGHT: 720,
  VIDEO_MAX_DURATION_SECONDS: 30,

  AUDIO_CHAT_MAX_BYTES: 25 * 1024 * 1024,
  AUDIO_BITRATE_KBPS: 64,
} as const;

export const STORAGE_BUCKET = 'andamento-obra';

export const SIGNED_URL_TTL_SECONDS = 1800;

export const OUTBOX_DEFAULTS = {
  MAX_ATTEMPTS: 5,
  BACKOFF_STEPS_MS: [1_000, 5_000, 15_000, 60_000] as const,
} as const;

export const CHAT_LIMITS = {
  MAX_ATTACHMENTS_PER_MESSAGE: 5,
  MAX_CONTENT_LENGTH: 4000,
  MESSAGES_PER_PAGE: 50,
  SIGNED_URL_CACHE_MS: 25 * 60 * 1_000,
} as const;

export const POLE_LIMITS = {
  MAX_PHOTOS_PER_INSTALLATION: 5,
  MAX_NOTES_LENGTH: 1000,
  LOGICAL_GRID_SIZE: 6000,
  GPS_TIMEOUT_MS: 10_000,
  GPS_LOW_ACCURACY_METERS: 50,
  PDF_CACHE_MAX_AGE_MS: 24 * 60 * 60 * 1_000,
} as const;

export const QUERY_DEFAULTS = {
  STALE_TIME_MS: 5 * 60 * 1_000,
  RETRY_COUNT: 2,
} as const;
